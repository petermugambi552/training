#include <iostream>
#include <vector>
using namespace std;

int main()
{
    // array example
    int arr[] = {10, 20, 30, 40, 50};

    int n = sizeof(arr) / sizeof(arr[0]);
    cout << "Array elements are: ";
    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << " ";
        cout
    }
    return
}